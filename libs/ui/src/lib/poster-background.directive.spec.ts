import { PosterBackgroundDirective } from './poster-background.directive';

describe('PosterBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PosterBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
